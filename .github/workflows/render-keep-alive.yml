name: Keep Render API Alive

on:
  schedule:
    - cron: "*/14 * * * *"   # har 14 minute
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping CET Ranker API
        run: |
          curl -I https://api.cetranker.com/web
